# Version floors, not hard pins: a hard pin (psycopg==3.2.3) stopped resolving
# on newer Python releases and broke the build. Compatible-release ranges keep
# the deploy working while staying inside the tested major version.
fastapi>=0.115,<0.120
uvicorn[standard]>=0.34,<0.40
sqlalchemy>=2.0,<2.1
psycopg[binary]>=3.2,<4
python-telegram-bot[job-queue]>=21.9,<22
apscheduler>=3.11,<4
pydantic>=2.10,<3
python-dotenv>=1.0,<2
